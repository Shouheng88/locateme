package me.shouheng.locate.engine.notify

import me.shouheng.locate.engine.keyword.SearchKeywords

/** The default implementation of notifier by email. */
class EmailNotifier: ILocateNotifier {
    override fun notify(result: SearchKeywords) {

    }
}
